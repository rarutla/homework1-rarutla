What is the difference between a merge and a rebase?

A merge is non-destructive while a rebase is not. A rebase will re-write the project history. 



What is a remote repository?

A remote repository is a repository hosted on a server that is accessible to multiple users.